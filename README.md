Pi Camera is a client and server for connecting multiple Raspberry Pi camera and computer modules over WiFi (lan), or Wireguard (Non-lan).

The server spawns a web interface that updates based on camera connections, and the client connections to a command-line supplied ip address.
